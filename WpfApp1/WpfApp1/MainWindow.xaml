<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="250"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Label>Path:</Label>
        <TextBox Name="tbPath" Grid.Column="1"></TextBox>
        <Button Click="SourceBtn" Grid.Column="2">Source</Button>

        <Label Grid.Row="1">URL:</Label>
        <TextBox Name="tbURL" Grid.Row="1" Grid.Column="1"></TextBox>
        <Button Click="DownloadBtn" Grid.Row="1" Grid.Column="2">Download</Button>

        <ListBox Name="lbFiles" Grid.Row="2" Grid.ColumnSpan="3" Margin="10" BorderBrush="Black" BorderThickness="1" MouseDoubleClick="lbFiles_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding FileName}" MaxWidth="150" TextWrapping="Wrap"></TextBlock>
                        <ProgressBar Value="{Binding Percentage}" MinWidth="200" Grid.Column="1" Height="20" Margin="5,0"></ProgressBar>
                        <TextBlock Text="{Binding Percentage}" Grid.Column="2" Width="100" Height="20" Margin="5,0"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Window>
